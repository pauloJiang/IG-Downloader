# 不要将 Token 写入本地文件。
# 请在启动前通过环境变量注入，例如：
#
#   export BOT_TOKEN=your_token_here        # Linux / macOS
#   $env:BOT_TOKEN="your_token_here"       # Windows PowerShell
#
# Docker Compose 会从当前 shell 读取 BOT_TOKEN（见 README）。
